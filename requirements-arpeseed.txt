# ARPESeed local environment — inference + training scripts
# Install: source "ARPESeed venv/bin/activate" && pip install -r requirements-arpeseed.txt

numpy>=1.24
torch>=2.2
torchvision>=0.17
matplotlib>=3.8
