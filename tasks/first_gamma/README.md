# first-gamma — locate Γ in noisy isoenergy stacks

**Artifact:** `arpeseed/first-gamma-v2`

- [Model card](MODEL_CARD.md)
- [OOD benchmark results](BENCHMARK_RESULTS.json)

Weights (`arpeseed_first_gamma_v2.pth`) are not in git — obtain from release or train with `arpeseed_train_eval.py`.
